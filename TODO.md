schema create is broken -> unknown reason

use cases are not clear, need example schema and data to go with it (manager -> employee???)

field validation / allowed values:
number or string
number: range of allowable values, integer / decimal
string: min characters, max characters, multi-line(different type?)
definition references: starting with inside schema only, how do we define those(ui + actual implementation of the data)

v: look at / explore / do this

good things:
CSS colors for input field / clickable link / labels are awesome

bad things:
need toast for errors
flow w/flash messages for 'did the thing, boss'

data store needs rethinking - how should it work once we have A Lot of data?
for values of A Lot:
  thousands of schemas
  thousands of documents within a given schema

