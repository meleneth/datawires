<%= turbo_stream.replace "ribbon" do %>
  <%= render "ribbon",
             domain: @domain,
             document: @document,
             draft: @draft,
             ptr: @ptr,
             mode: @mode %>
<% end %>

<%= turbo_stream.replace "editor" do %>
  <%= render "editor",
             domain: @domain,
             document: @document,
             draft: @draft,
             ptr: @ptr,
             mode: @mode %>
<% end %>
