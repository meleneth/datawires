= turbo_frame_tag "edit_affordance_builder_editor" do
  = render CardComponent.new(html_options: { class: "w-full" }) do
    = render CardContentComponent.new do
      .space-y-1
        = render TypographyComponent.new(as: :h3, text: "Inline editor")
        = render TypographyComponent.new(as: :muted, text: "Open a row or field to refine it here.")
