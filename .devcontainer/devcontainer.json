{
  "name": "datawires",
  "dockerComposeFile": "docker-compose.yml",
  "service": "app",
  "workspaceFolder": "/workspaces/datawires",
  "runServices": ["app", "db"],
  "remoteUser": "vscode",
  "updateRemoteUserUID": true,
  "forwardPorts": [3000],
  "postCreateCommand": "sh -lc 'sudo chown -R vscode:vscode /usr/local/bundle && until pg_isready -h db -U datawires >/dev/null 2>&1; do sleep 1; done && (bundle check || bundle install) && bin/rails db:prepare'",
  "postStartCommand": "sh .devcontainer/start-dev-server.sh",
  "customizations": {
    "vscode": {
      "extensions": [
        "Shopify.ruby-lsp"
      ]
    }
  }
}
