build-job:
  image: node:18-bullseye
  stage: build
  script:
    - yarn
    - yarn build
  artifacts:
    paths:
      - dist/*
    expire_in: 1 week

test-job1:
  image: node:18-bullseye
  stage: test
  script:
    - echo sed -i 's/pouchdb-browser/pouchdb-node/g' package.json
    - echo sed -i 's/pouchdb-browser/pouchdb-node/g' ./src/store/index.coffee
    - echo sed -i 's/pouchdb-browser/pouchdb-node/g' ./src/store/actions.coffee
    - yarn
    - yarn test:unit

deploy-prod:
  stage: deploy
  script:
    - echo "This job deploys something from the $CI_COMMIT_BRANCH branch."
